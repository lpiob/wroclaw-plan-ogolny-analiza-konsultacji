# Struktura

- `download/` - skrypty do pobierania danych źródłowych
- `prepare/` - skrypty do wstępnego przetworzenia danych źródłowych
- `analyze/` - skrypty do właściwej analizy danych

Do działania skryptów wymagane są zależności wymienione w `../requirements.txt`.

